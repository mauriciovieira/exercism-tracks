version: "3"
services:
  exercism-go:
    build: go
    volumes:
      - ./go:/go
  exercism-python:
    build: python
    volumes:
      - ./python:/python
  exercism-ruby:
    build: ruby
    volumes:
      - ./ruby:/ruby
